#include <iostream>
#include <string>
#include <algorithm> // Include the algorithm header

using namespace std;

int main() {
    string word;
    cin>>word;
    reverse(word.begin(), word.end());
    cout << word << endl;
    return 0;
}
